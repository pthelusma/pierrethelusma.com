node {

    currentBuild.displayName = "1.0.0.0"
    
    stage('Checkout') {
        echo "Checking out source control"
        checkout scm
    }

    stage('Build') {
        echo "Building from source"
        checkout scm
    }
}